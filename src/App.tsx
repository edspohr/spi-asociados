export default function App() {
  return (
    <main className="mx-auto flex min-h-screen max-w-5xl flex-col gap-4 p-6">
      <h1 className="text-2xl font-semibold text-[color:var(--color-primary)]">
        SPI Americas — Hoja de Vida de Asociados
      </h1>
      <p className="text-sm text-[color:var(--color-text-muted)]">
        Andamiaje inicial. Las siguientes fases construirán el formulario.
      </p>
    </main>
  );
}
